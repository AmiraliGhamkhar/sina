<UserControl x:Class="MedicalScribe.WPF.Views.RecorderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Recorder" Style="{StaticResource SectionTitle}"/>
            <Border Background="#FFF7ED" BorderBrush="#FDBA74" BorderThickness="1" CornerRadius="8" Padding="12,8">
                <TextBlock Text="{Binding CapabilityBanner}" Foreground="#9A3412" FontSize="12.5"/>
            </Border>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,14,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="190"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Microphone" Style="{StaticResource FieldLabel}"/>
                    <ComboBox ItemsSource="{Binding Microphones}" SelectedItem="{Binding SelectedMicrophone}"
                              DisplayMemberPath="Name" MinWidth="260" HorizontalAlignment="Left"/>
                    <TextBlock Text="no devices listed until Phase 2 capture service" Style="{StaticResource Hint}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="24,0">
                    <TextBlock Text="{Binding StateLabel}" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Elapsed, StringFormat=hh\\:mm\\:ss}" FontSize="26" FontFamily="Consolas"
                               HorizontalAlignment="Center" Foreground="{StaticResource Accent}"/>
                    <ProgressBar Width="170" Height="8" Margin="0,8,0,0" Value="{Binding AudioLevel, Mode=OneWay}" Maximum="1"/>
                    <TextBlock HorizontalAlignment="Center" Style="{StaticResource Hint}" Text="audio level"/>
                </StackPanel>

                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <Button Content="●  Start" Command="{Binding StartCommand}" Margin="0,0,0,8"/>
                    <Button Content="⏸  Pause" Style="{StaticResource SecondaryButton}" Command="{Binding PauseCommand}" Margin="0,0,0,8"/>
                    <Button Content="▶  Resume" Style="{StaticResource SecondaryButton}" Command="{Binding ResumeCommand}" Margin="0,0,0,8"/>
                    <Button Content="■  Stop" Style="{StaticResource DangerButton}" Command="{Binding StopCommand}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding ConnectionLabel}" Foreground="{StaticResource MutedInk}" FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,14,0,0" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Last error" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding LastError}" Foreground="{StaticResource Danger}" Margin="0,4,0,0"
                           Style="{StaticResource Hint}"/>
                <TextBlock Text="Global hotkey Ctrl+Alt+Space toggles recording (configurable in User Settings)."
                           Style="{StaticResource Hint}" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
