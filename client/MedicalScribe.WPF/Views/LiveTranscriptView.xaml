<UserControl x:Class="MedicalScribe.WPF.Views.LiveTranscriptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Live transcript" Style="{StaticResource SectionTitle}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Content="Delete last sentence" Style="{StaticResource SecondaryButton}" Command="{Binding DeleteLastSentenceCommand}"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding StatusNote}" Foreground="{StaticResource MutedInk}" Margin="0,0,0,10"/>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox Grid.Row="0" ItemsSource="{Binding Segments}" SelectedItem="{Binding SelectedSegment}"
                         BorderThickness="0" Background="Transparent"
                         HorizontalContentAlignment="Stretch"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Timestamp}" Foreground="{StaticResource MutedInk}" FontFamily="Consolas" FontSize="12" VerticalAlignment="Top"/>
                                <TextBox Grid.Column="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="34" BorderThickness="0,0,0,1"
                                         Background="Transparent" FontSize="14"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <Border Grid.Row="1" BorderBrush="{StaticResource Hairline}" BorderThickness="0,1,0,0" Padding="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="interim (live)" FontSize="11" Foreground="{StaticResource MutedInk}"/>
                        <TextBlock Text="{Binding InterimText}" FontStyle="Italic" Foreground="{StaticResource Accent}" FontSize="14" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <ItemsControl Grid.Row="3" ItemsSource="{Binding Warnings}" Margin="0,10,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="6" Padding="10,6" Margin="0,3">
                        <TextBlock Foreground="#991B1B" FontSize="12.5">
                            <Run Text="{Binding Code, Mode=OneWay}"/><Run Text=" — "/><Run Text="{Binding Message, Mode=OneWay}"/>
                        </TextBlock>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
