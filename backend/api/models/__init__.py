"""ORM entities — Phase 7.

Planned table set (see docs/ARCHITECTURE.md §Data model):
User, Role, Patient, Encounter, Transcript, TranscriptSegment, Report,
ReportTemplate, AIProvider, AIRequest, AuditLog, Setting.

Kept empty until then on purpose: the API surface in Phase 1/2 must be
buildable and testable without a database, and half-wired ORM code is worse
than none.
"""
